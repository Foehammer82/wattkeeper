[project]
name = "wattkeeper-docs"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
docs = [
  "mkdocs>=1.6,<2.0",
  "mkdocs-material>=9.5,<10.0",
  "mkdocs-git-revision-date-localized-plugin>=1.2,<2.0",
]

[tool.uv]
package = false